'use strict'

import { CalendarEvent } from './calendar-event.js'

describe('Rocketrip Code Test', () => {
  describe('CalendarEvent', () => {
    it('has a date', () => {
      expect(true).toBe(false)
    })

    it('has a title', () => {
      expect(true).toBe(false)
    })

    describe('#formatted_time', () => {
      it('returns the date as a readable time span', () => {
        expect(true).toBe(false)
      })
    })
  })
})
